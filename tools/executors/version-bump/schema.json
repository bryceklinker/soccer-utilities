{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "cli": "nx",
  "properties": {
    "bump": {
      "type": "string",
      "description": "Kind of bump to use: patch, minor, major"
    }
  }
}
